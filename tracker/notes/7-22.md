1. Need to create a better distinction between the capabilities (like features including Code Repo, Package Repo, CI/CD, Code Quality Checks, and Code Security Checks) on GitHub vs the AWS Equivalent. So it needs to be a five column table with Capability, GitHub, AWS Equivalent, Cost, Notes. Ali also wants to know what Claude Code itself can cover. One of the things that was brought up in the meeting was secret protection like when you push code to a repo, Claude will detect that you have something you're not supposed to and be like "Hey, I wont let you push this because if you didn't notice, you're leaking your API key {or} Secret {or} something that you don't want to be on the repo." Another thing would be if we can set up environment variables within these two products and use them directly in code. This way, they can be managed directly from the product and not on a local machine or something in the cloud like an EC2. We *could* always do it that way but it would be nice to do it this way too if it's safe. 

2. We need to evaluate how we want to go the direction of using Bedrock as our AI source. So I have found a few ways we can do it...:
    - Whats cool is that with in Claude Desktop, you can connect your bedrock account to claude using the developer settings by going to Help -> Troubleshooting -> Enable Developer Mode; then you let the program reboot then go to Developer -> Configure Third-Party Interface -> Connection and then select Bedrock AWS. With this, you can run all the tokens through bedrock instead of paying for claude, I'm pretty sure. You can also link this bedrock account to the claude code app, claude code CLI, and Claude Code extension in Vs Code. I'd also like to know the price difference here doing it this way instead of just paying for claude enterprise and the token usage by the API rate. Is this way cheaper? Biggest downside here is that we can't use other models in the Claude desktop app. Another thing I need to know for sure is if we need to onboard Anthropic as a vendor or if we're fine. If everything is getting passed to bedrock, we're fine because we already onboarded AWS as a vendor. Claude Desktop, Code, and extension should just be the tool we're using like any other OSS tool.
    - I was thinking to get around this limitation we could use something like LibreChat which would, in theory from my research, allow us to hook our AWS Bedrock account up to it and have that chat interface for all types of models, not just Anthropic. This is another OSS tool we need to onboard. I'm not sure there is an AWS alternative to this but if there is, I'd like to also know about that.
    - For the overall setup of it, this is my idea. For testing, we would use our Sandbox OU and in sandbox OU we would create a test account called something like `sandbox_ai` or something like that. Then we could, however it is done configure bedrock that way and test all these features I am talking about above. For prod, my idea is that we'd have to create accounts for each individual person so we can track their token consumption. I'm not sure if you can create one account and have separate token consumption that way. Another question or thought is if we should create a separate OU for this. We already have OUs for security, infra, AI/ML dev, and Data eng so should we create an OU called `AI_Access` or something?
    - I'd also need to know what other plugin options are open to us going down the AWS Bedrock route. I want to know all the ways we can take advantage of this method.
